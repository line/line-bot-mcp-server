{
  "name": "@line/line-bot-mcp-server",
  "version": "0.0.1-local",
  "description": "MCP server for interacting with your LINE Official Account",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "module": "./dist/index.js",
  "bin": {
    "line-bot-mcp-server": "./dist/index.js"
  },
  "files": [
    "dist",
    "richmenu-template"
  ],
  "scripts": {
    "generate:tools": "tsx scripts/sync-tool-artifacts.ts",
    "clean": "rm -rf dist/*",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier -l \"**/*.ts\"",
    "typecheck": "tsc --project tsconfig.test.json && tsc --project tsconfig.scripts.json",
    "lint": "npm run format:check && npm run typecheck",
    "compile": "npm run clean && tsc && shx chmod +x dist/*.js",
    "build": "npm run generate:tools && npm run lint && npm run compile",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:tools": "npm run generate:tools && git diff --exit-code",
    "check:publint": "publint",
    "release": "npm run build && npm publish --provenance --access public"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:line/line-bot-mcp-server.git"
  },
  "keywords": [
    "line",
    "bot",
    "mcp"
  ],
  "homepage": "https://github.com/line/line-bot-mcp-server",
  "bugs": "https://github.com/line/line-bot-mcp-server/issues",
  "dependencies": {
    "@line/bot-sdk": "^10.0.0",
    "@marp-team/marp-cli": "^4.2.3",
    "@marp-team/marp-core": "^4.1.0",
    "@modelcontextprotocol/sdk": "^1.8.0",
    "puppeteer": "^24.27.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "prettier": "3.8.1",
    "publint": "0.3.18",
    "shx": "^0.4.0",
    "tsx": "^4.19.3",
    "typescript": "^6.0.2",
    "vitest": "^4.0.0"
  },
  "license": "Apache-2.0"
}
